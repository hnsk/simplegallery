<nav class="breadcrumbs" aria-label="Breadcrumb"><ol>
{% for crumb in breadcrumbs %}
{% if crumb.href %}<li><a href="{{ crumb.href }}">{{ crumb.name }}</a></li>{% else %}<li><span aria-current="page">{{ crumb.name }}</span></li>{% endif %}
{% endfor %}
</ol></nav>
